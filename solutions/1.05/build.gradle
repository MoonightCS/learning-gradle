
task stringsAndTypes {
    doLast {

        println "Nice work abbreviating the task name"
        def foo = 4.2
        println "foo class is ${foo.class}, and its value is: $foo"
        println "square root of 127 is ${Math.sqrt(127)}"
        foo = new Date()
        println "foo class is ${foo.class} and its value is: $foo"

        def hello = "Hello, Jeremy."
        def jeremyName = hello.substring(7, 13)
        println "${jeremyName}"
        println "${jeremyName.toUpperCase()}"

    }
}

task closures {
    doLast {
        def addTwo = { arg1, arg2 -> arg1 + arg2 }
        println addTwo.call(17, 25)
    }
}

task lists {
    doLast {
      def myList = ["Donut", "Eclair", "Froyo", "Gingerbread", "Honeycomb"]
      def candiesUpperCase = myList.collect{it.toUpperCase()}
      candiesUpperCase.each{println it}
    }
}

task veryHardTask {
  doLast {
    def foo = "bar"
    println "$foo + foo = ${foo + "foo"}"
  }
}
